node_modules
.next
uploads
*.log
tsconfig.tsbuildinfo

# Segreti: nel repo va solo il modello, .env.example.
.env
.env.*
!.env.example

# Il certificato e la sua chiave sono di questa installazione: si generano con
# certificati/genera.sh, non si pubblicano.
certificati/*.pem
certificati/openssl.cnf

# I dump del database: dentro ci sono i dati delle persone — certificati
# medici, recapiti, impronte delle password. Restano sulla macchina, non nel
# repository, e non si pubblicano da nessuna parte.
backup/
*.sql.gz
